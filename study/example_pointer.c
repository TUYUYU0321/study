// 문제 1

// *와 & 연산자의 역할이 무엇인지 말해보세요

// * : 해당 주솟값에 위치한 변수를 언급

// & : 해당 변수의 주솟값을 표시


//문제 2

// int **a; 와 같은 이중 포인터에 대해 생각해 보세요

// 이는 int* *a;라고 생각할 수 있다. 이 때, *a는 a라는 주솟값에 들어있는 변수이다.

// 그럼 이제 int *(*a)라고 생각해보자.

// a라는 주솟값에 들어있는 변수를 주솟값으로 가지는 변수가 된다.

// 즉, a라는 주소에는 또 다른 변수의 주솟값이 들어있다.

// ** 메모리 동적 할당에 사용된다고 다른 사람이 언급함